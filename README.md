# LabFlow

## Docker development

Start MongoDB, all backend services, and the API gateway with:

```bash
docker compose up --build
```

Once the services are healthy, the gateway is available at http://localhost:3000.
